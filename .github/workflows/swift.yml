name: CI
on: [push]
jobs:
      build:
        runs-on: macos-latest

        steps:
        - name: Checkout
          uses: actions/checkout@master
      #- name: Testing....
      # run: ls /Applications | grep Xcode
        - name: iPhone 11 Test
          run: xcodebuild build -workspace ScreenSaver.xcworkspace -scheme ScreenSaver -destination 'platform=iOS Simulator,name=iPhone 11,OS=13.1'
